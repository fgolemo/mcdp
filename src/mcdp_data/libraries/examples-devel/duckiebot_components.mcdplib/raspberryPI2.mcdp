mcdp {
    provides computation [flops]

    requires in [`DC_power]
    (required in).connector >= `barrel_connectors: barrel_4mm_1_7mm
    (required in).voltages >= `DC_voltages: v5

    requires cost [$]
    required cost >= 30.00 $

    computation <= 1000 Mflops

    c0 = 100 mA
    c = 0.001 mA/flops
    amps = c0 + computation  * c
    (required in).amps >= amps
}
