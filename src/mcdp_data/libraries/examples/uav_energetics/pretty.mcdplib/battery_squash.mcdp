mcdp {
    battery = new Batteries

    provides capacity <= battery.capacity
    provides missions <= battery.missions

    requires mass >= battery.mass

    labor_per_replacement = 10 CHF 
    labor_cost = battery.maintenance * labor_per_replacement
    
    requires cost >= battery.cost + labor_cost
}