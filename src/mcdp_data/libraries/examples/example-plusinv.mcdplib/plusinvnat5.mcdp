mcdp {
	variable x, y [Nat]
    provides z [Nat]

	x + y >= ceil(sqrt(x)) + ceil(sqrt(y)) + provided z 

	requires x, y
}

# Note: this is a difficult one
# x + y >= sqrt(x) + sqrt(y) + 5

# z = 10
# a >= x + 10
# x + y >= a 
# x + y >= x + 10
# y >= 10