# this has a (4,4) isolated solution!
# x + y >= ceil(sqrt(x)) + ceil(sqrt(y)) + 4
mcdp {
    provides z [s]
    variable x [s]
    variable y [s]
    x + y  >= ceil(sqrt(x)) + ceil(sqrt(y)) + provided z 

    requires x >= x
    requires y >= y
}