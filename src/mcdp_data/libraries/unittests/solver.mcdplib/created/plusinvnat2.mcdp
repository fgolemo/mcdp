mcdp {
	f = instance mcdp  {
	    provides a [Nat]
	    
	    requires x [Nat]
	    requires y [Nat]
	    
	    x + y >= a
	}

	provides z [Nat]

	f.a >= ceilsqrt(f.x) + ceilsqrt(f.y) + z 

	requires x, y for f
}
 