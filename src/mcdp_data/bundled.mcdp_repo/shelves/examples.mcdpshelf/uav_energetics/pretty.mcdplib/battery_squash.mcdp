mcdp {
    battery = instance `Batteries

    provides capacity <= capacity provided by battery
    provides missions <= missions provided by battery

    requires mass >= mass required by mass

    labor_per_replacement = 10 CHF
    labor_cost = maintenance required by battery * labor_per_replacement

    requires cost >= cost required by battery + labor_cost
}
