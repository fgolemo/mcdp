template mcdp {
    provides pisocketcom1 [`PISocketCom]
    provides out1 [`PWM]
    provides out2 [`PWM]
    provides out3 [`PWM]
    provides out4 [`PWM]

    #AdafruitDCHatCore.mcdp

    requires pisocketcom1 [`PISocketCom]

    #core.pisocketcom1 <= required pisocketcom1
}