# not_implemented_error
mcdp {
    variable a, b, c, d, E, f [Rcomp]

    a ≽ 0.50 * min(b, f)
    a ≽ 0.50 * min(c, d)
    b ≽ 0.20
    c ≽ 0.45
    c ≽ 0.30
    d ≽ 1.00
    E ≽ 0.50
    f ≽ 0.90 * E

    requires objective = min(a, max(c, E))
}
