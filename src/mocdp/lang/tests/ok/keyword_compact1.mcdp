# test connected
compact mcdp {  
      sub A = template mcdp {
        requires speed [R]
        requires torque [R]
      }
      
      sub B = template mcdp {
        provides speed [R]
        provides torque [R]
      }
      
      A.speed <= B.speed
      A.torque <= B.torque
          
}