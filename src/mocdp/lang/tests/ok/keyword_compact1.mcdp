# test connected
compact mcdp {  
      sub A = instance template mcdp {
        requires speed [R]
        requires torque [R]
      }
      
      sub B = instance template mcdp {
        provides speed [R]
        provides torque [R]
      }
      
      A.speed <= B.speed
      A.torque <= B.torque
          
}