# test semantic_error
mcdp {
  sub motor = abstract cdp {
    provides capacity [W]
    requires mass [g]

  sub battery = dp {
    provides capacity [J]
    requires mass [kg]

    implemented-by load BatteryDP
  }
    capacity <= battery.torque 
    battery.mass <= mass
}}
