# test connected
mcdp {  
    sub f =  instance template mcdp {
        provides x [R]
        requires y [R]

    }
    sub g = instance template mcdp {
        provides a [R]
        requires x [R]
    }
    # conversion from int
    f.x  >= f.y + g.x + 4 [R]
    g.a >= f.y 
  }