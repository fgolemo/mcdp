# test connected
mcdp {  
    sub f =  template mcdp {
        provides x [R]
        requires y [R]

    }
    sub g = template mcdp {
        provides a [R]
        requires x [R]
    }
    # conversion from int
    f.x  >= f.y + g.x + 4 [R]
    g.a >= f.y 
  }