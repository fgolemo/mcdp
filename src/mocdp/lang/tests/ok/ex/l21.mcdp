# test connected
mcdp {
    sub times = instance mcdp {
        provides a [R]
        provides b [R]
        requires c [R]
 
        c >= a * b
    }

    times.a >= times.c
    times.b >= times.c 
}