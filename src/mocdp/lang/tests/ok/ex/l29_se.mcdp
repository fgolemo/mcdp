# test semantic_error
# Fails because of double constraint on DP.a 
mcdp {
  
    sub DP = cdp {
        provides a [R]
        provides b [R]
        requires c [R]
        c >= a * b
    }
   
   DP.a >= DP.c
   DP.a >= DP.c
  }