# test connected
compact cdp {  
      A = template cdp {
        requires speed [R]
        requires torque [R]
      }
      
      B = template cdp {
        provides speed [R]
        provides torque [R]
      }
      
      A.speed <= B.speed
      A.torque <= B.torque
          
}