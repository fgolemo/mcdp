# test connected
mcdp {
    sub times = instance mcdp {
        provides a [dimensionless]
        provides b [dimensionless]
        requires c [dimensionless]
 
        c >= a * b
    }

    times.a >= times.c
    times.b >= times.c 
}