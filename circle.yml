dependencies:
  pre:
    - sudo apt-get update -y
#    - sudo apt-get install -y libgvc6
    - sudo apt-get install --fix-missing graphviz 
    - sudo apt-get install python-numpy python-matplotlib python-yaml python-pip wkhtmltopdf

test:
  override:
    - make clean comptests-run-parallel