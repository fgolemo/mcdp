cdp {  
          f =  cdp {
            provides a [R]
            provides b [R]
            #    
            requires c [R]
            c >= 2 [R] * (a * b)
          }

      f.a >= f.c
      f.b >= f.c
  }