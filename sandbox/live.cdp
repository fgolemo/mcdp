cdp {

	F = abstract cdp {
  requires a [R]
  requires b [R]
  
  provides c [R]
  
  	x = c * 1.0 [R]
    x <= a * b
  }

  F.c >= max( square(F.a) , 1.0 [R])

  requires a >= F.a + 3 [R]
  requires b for F

}